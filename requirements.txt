# Core — all free, no API keys.
yt-dlp>=2024.1.0            # keyless YouTube search + (future) audio download
youtube-transcript-api>=0.6.2  # captions without downloading video
requests>=2.28.0           # talks to local Ollama
rich>=13.0.0               # nicer CLI output (optional; CLI degrades without it)
fastapi>=0.110.0           # web UI backend
uvicorn[standard]>=0.29.0  # ASGI server for the web UI

# Optional — Whisper audio fallback (MQ_WHISPER_FALLBACK=1). Transcribes a
# video's audio locally when captions are missing OR YouTube blocks the caption
# endpoint. No system ffmpeg needed (PyAV is bundled). Uncomment to enable:
# faster-whisper>=1.0.0
